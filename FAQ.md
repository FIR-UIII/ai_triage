'finish_reason': 'length', что означает остановку генерации по максимальной длине (max_tokens), а не по естественному завершению.
нужно увеличить параметр n_ctx вызова LLM

finish_reason: 'stop' – модель завершила генерацию сама, а не из-за ограничения длины.